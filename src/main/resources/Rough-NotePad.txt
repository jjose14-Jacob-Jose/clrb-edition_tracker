    $(document).ready(function() {
        $('#formUserInput').submit(function(event) {
            event.preventDefault();
            let formData = $(this).serialize();

            $.ajax({
                url: URL_GENERATE_SUMMARY,
                type: URL_GENERATE_SUMMARY_REQUEST_TYPE,
                data: formData,
                dataType: 'json',
                success: function(response) {
                    // Update the page content using the response data
                    displayAPIResponseInHTML(response);
                    responseFromAPI = response;
                },
                error: function(error) {
                    console.log(error);
                    printToAlert(error);
                }
            });
        });
    });


    When I debug the above JS code, after clicking the form's 'submit' button, the code inside '$.ajax' is not executed. When I click the submit button second time, the code is executed.